type ActionCounter @model @auth(rules: [{allow: public}]) {
  id: ID!
  counter: Int
}

type Article @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  description: String
  text: String
  published: Boolean
}
 